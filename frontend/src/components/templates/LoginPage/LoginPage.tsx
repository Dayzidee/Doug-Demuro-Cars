const LoginPage = () => {
  return (
    <div>
      <h1 className="text-3xl font-heading">Login</h1>
      <p className="mt-4">This is the login page.</p>
      <button className="mt-4 p-2 rounded bg-primary-gradient text-white">
        Log In
      </button>
    </div>
  );
};

export default LoginPage;
